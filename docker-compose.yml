services:
  ui:
    build: .
    image: ${FULL_IMAGE:-26fa-ui}:${IMAGE_TAG:-latest}
    container_name: ${CONTAINER_NAME:-26fa-ui}
    ports:
      - "${APP_PORT:-8080}:80"
    restart: unless-stopped
